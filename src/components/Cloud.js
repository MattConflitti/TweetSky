import React, { Component } from 'react';
import {
    View
} from 'react-native';

class Cloud extends Component {
    render() {
        return (
            <View>
                {/* Take data from state and dynamically create <Text> tags */}
            </View>
        );
    }
}

const styles = StyleSheet.create({});

Cloud.propTypes = {};

export { Cloud };